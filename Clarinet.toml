[project]
name = 'decentralized-identity-verification-system'
description = ''
authors = []
telemetry = true
cache_dir = '.\.cache'
requirements = []
[contracts.decentralized-identity-verification-system]
path = 'contracts/decentralized-identity-verification-system.clar'
clarity_version = 2
epoch = 2.5

[contracts.zk-identity-challenge]
path = 'contracts/zk-identity-challenge.clar'
clarity_version = 2
epoch = 2.5
[contracts.BiometricVerificationSimulator]
path = 'contracts/BiometricVerificationSimulator.clar'
clarity_version = 2
epoch = 2.5
[repl.analysis]
passes = ['check_checker']

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
